apiVersion: apps/v1
kind: Deployment
metadata:
  name: genai-service
  labels:
    app: genai-service
spec:
  selector:
    matchLabels:
      app: genai-service
  replicas: 2
  revisionHistoryLimit: 1
  template:
    metadata:
      labels:
        app: genai-service
    spec:
      imagePullSecrets:
      - name: regcred
      containers:
      - name: genai-service
        image: ghcr.io/stefanfilkov/petclinic/genai-service:latest
        imagePullPolicy: "Always"
        resources:
          limits:
            memory: "512Mi"
        envFrom:
        - configMapRef:
            name: genai-service-configmap
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: genai-service
  labels:
    app: genai-service
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    name: "genai-service"
  selector:
    app: genai-service
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: genai-service
  annotations:
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/ssl-passthrough: "true"
    nginx.ingress.kubernetes.io/proxy-buffer-size: "16k"
    nginx.ingress.kubernetes.io/proxy-body-size: 50m
    nginx.ingress.kubernetes.io/server-tokens: "false"
    nginx.ingress.kubernetes.io/fail-timeout: "45s"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - genai-service.domashno.party
      secretName: wild-tinqin-prp-be-ys-net
  rules:
    - host: genai-service.domashno.party
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: genai-service
                port:
                  number: 8080
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: genai-service-configmap
data:
