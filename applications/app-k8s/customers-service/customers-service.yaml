apiVersion: apps/v1
kind: Deployment
metadata:
  name: customers-service
  labels:
    app: customers-service
spec:
  selector:
    matchLabels:
      app: customers-service
  replicas: 2
  revisionHistoryLimit: 1
  template:
    metadata:
      labels:
        app: customers-service
    spec:
      imagePullSecrets:
      - name: regcred
      containers:
      - name: customers-service
        image: ghcr.io/stefanfilkov/petclinic/customers-service:latest
        imagePullPolicy: "Always"
        resources:
          limits:
            memory: "512Mi"
        envFrom:
        - configMapRef:
            name: customers-service-configmap
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: customers-service
  labels:
    app: customers-service
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    name: "customers-service"
  selector:
    app: customers-service
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: customers-service
  annotations:
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/ssl-passthrough: "true"
    nginx.ingress.kubernetes.io/proxy-buffer-size: "16k"
    nginx.ingress.kubernetes.io/proxy-body-size: 50m
    nginx.ingress.kubernetes.io/server-tokens: "false"
    nginx.ingress.kubernetes.io/fail-timeout: "45s"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - customers-service.domashno.party
      secretName: wild-tinqin-prp-be-ys-net
  rules:
    - host: customers-service.domashno.party
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: customers-service
                port:
                  number: 8080
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: customers-service-configmap
data:
