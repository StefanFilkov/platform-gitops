apiVersion: apps/v1
kind: Deployment
metadata:
  name: config-server
  labels:
    app: config-server
spec:
  selector:
    matchLabels:
      app: config-server
  replicas: 2
  revisionHistoryLimit: 1
  template:
    metadata:
      labels:
        app: config-server
    spec:
      imagePullSecrets:
      - name: regcred
      containers:
      - name: config-server
        image: ghcr.io/stefanfilkov/petclinic/config-server:latest
        imagePullPolicy: "Always"
        resources:
          limits:
            memory: "512Mi"
        envFrom:
        - configMapRef:
            name: config-server-configmap
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: config-server
  labels:
    app: config-server
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    name: "config-server"
  selector:
    app: config-server
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: config-server
  annotations:
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/ssl-passthrough: "true"
    nginx.ingress.kubernetes.io/proxy-buffer-size: "16k"
    nginx.ingress.kubernetes.io/proxy-body-size: 50m
    nginx.ingress.kubernetes.io/server-tokens: "false"
    nginx.ingress.kubernetes.io/fail-timeout: "45s"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - config-server.domashno.party
      secretName: wild-tinqin-prp-be-ys-net
  rules:
    - host: config-server.domashno.party
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: config-server
                port:
                  number: 8080
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: config-server-configmap
data:
