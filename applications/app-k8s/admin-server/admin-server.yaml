apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-server
  labels:
    app: admin-server
spec:
  selector:
    matchLabels:
      app: admin-server
  replicas: 2
  revisionHistoryLimit: 1
  template:
    metadata:
      labels:
        app: admin-server
    spec:
      imagePullSecrets:
      - name: regcred
      containers:
      - name: admin-server
        image: ghcr.io/stefanfilkov/petclinic/admin-server:latest
        imagePullPolicy: "Always"
        resources:
          limits:
            memory: "512Mi"
        envFrom:
        - configMapRef:
            name: admin-server-configmap
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: admin-server
  labels:
    app: admin-server
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    name: "admin-server"
  selector:
    app: admin-server
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: admin-server
  annotations:
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/ssl-passthrough: "true"
    nginx.ingress.kubernetes.io/proxy-buffer-size: "16k"
    nginx.ingress.kubernetes.io/proxy-body-size: 50m
    nginx.ingress.kubernetes.io/server-tokens: "false"
    nginx.ingress.kubernetes.io/fail-timeout: "45s"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - admin-server.domashno.party
      secretName: wild-tinqin-prp-be-ys-net
  rules:
    - host: admin-server.domashno.party
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: admin-server
                port:
                  number: 8080
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: admin-server-configmap
data:
